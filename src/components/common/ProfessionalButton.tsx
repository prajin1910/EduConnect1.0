// This component is now replaced by the enhanced Button component
// Keeping for backward compatibility, but forwarding to new Button
import Button from './Button';

const ProfessionalButton = Button;

export default ProfessionalButton;